@model IEnumerable<CRUDAspNetCoreMVCWebAppComEFCore.Models.Empregados>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Codigo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Funcao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LocalizacaoEscritorio)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Codigo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Funcao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LocalizacaoEscritorio)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EmpregadosId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EmpregadosId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EmpregadosId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
